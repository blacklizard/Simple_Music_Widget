Elf add a small widget in standby when music is played. There is animation when start the player and exit the player 

Need this patch:

```
;Retrieve new track information with the help of elf
;Send event 0x3030 to elf with a pointer to tracks data
;(c) blacklizard
```